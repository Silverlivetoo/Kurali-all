# KuraliAll 重构计划

## 目标
1. **简化架构** - 只保留命令行版本 (Shell) 和 WebUI 版本 (Python)
2. **修复bug** - 解决已知问题，提升稳定性
3. **统一代码风格** - 确保一致性
4. **增强维护性** - 模块化设计，易于扩展

## 当前问题分析

### 1. 冗余版本
- `kuraliAll.py` (Python CLI) - 与Shell版本功能重复
- `kuraliAll-gui.py` (终端GUI) - 功能有限，使用不便
- `kuraliAll.sh` (Shell CLI) - 主要命令行接口
- `kuraliAll-webui.py` (WebUI) - 浏览器界面

### 2. 代码结构问题
- 模块化但存在重复逻辑
- Shell和Python版本分开维护，逻辑不一致
- 错误处理不统一
- 依赖检查逻辑复杂

### 3. Bug点
根据README.md提到的bug:
- RPM解压不支持XZ压缩的payload
- pacman .PKGINFO解析问题
- AppImage symlink命名错误
- 系统模块独立source时has_cmd未定义
- install.sh权限问题

## 重构方案

### 第一阶段：清理冗余代码
1. 删除 `kuraliAll.py` 和 `kuraliAll-gui.py`
2. 保留 `kuraliAll.sh` 作为主要命令行工具
3. 保留 `kuraliAll-webui.py` 作为图形界面
4. 更新安装脚本 `install.sh` 只安装Shell版本

### 第二阶段：修复bug
1. RPM解压：增强压缩格式支持
2. pacman包解析：修复格式兼容性
3. AppImage处理：统一命名规则
4. Shell模块：修复has_cmd检测逻辑
5. 权限设置：确保安装脚本正确设置权限

### 第三阶段：统一代码架构
1. WebUI调用Shell版本而非独立实现
2. 统一包处理逻辑到pkg-handler.mod
3. 标准化错误处理和日志
4. 简化模块加载机制

### 第四阶段：优化用户体验
1. 简化命令行参数
2. 改进WebUI界面
3. 增强依赖检查功能
4. 添加更多测试用例

## 实施步骤

### 第一步：清理文件
```bash
# 移除冗余文件
rm kuraliAll.py kuraliAll-gui.py

# 更新README.md和USAGE.md
```

### 第二步：修复核心bug
```bash
# 修复pkg-handler.mod中的RPM解压逻辑
# 修复system.mod中的has_cmd检测
# 修复install.sh权限设置
```

### 第三步：统一架构
```bash
# 让WebUI调用Shell版本
# 统一包格式检测和处理逻辑
# 标准化模块接口
```

### 第四步：测试验证
```bash
# 测试不同格式包安装
# 测试WebUI功能
# 验证跨发行版兼容性
```

## 预期结果
1. 更简洁的代码结构
2. 更稳定的功能
3. 更统一的用户体验
4. 更容易维护的架构