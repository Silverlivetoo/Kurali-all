# KuraliAll 统一架构设计

## 当前问题
WebUI (kuraliAll-webui.py) 独立实现了：
- 包格式检测
- 包解压
- 包安装
- 依赖检查
- 包管理
- 元数据处理

这与 Shell 版本 (kuraliAll.sh) 重复了大量代码。

## 重构目标
WebUI 应该作为前端调用 Shell 后端，而不是独立实现。

## 设计方案

### 1. 创建统一的API接口
在 Shell 版本中添加 JSON 输出功能，WebUI 通过子进程调用。

#### API命令：
```bash
kurali api list          # JSON格式列出所有包
kurali api install <包>  # JSON格式返回安装结果
kurali api info <包>     # JSON格式返回包详情
kurali api deps         # JSON格式返回依赖检查结果
```

### 2. WebUI调用流程
WebUI应该通过：
```python
import subprocess
result = subprocess.run(["kurali", "api", "install", filepath], 
                       capture_output=True, text=True)
data = json.loads(result.stdout)
```

而不是当前直接实现所有逻辑。

### 3. Shell API模块设计
在 kuraliAll.sh 中添加 `api` 命令：
```bash
cmd_api() {
    local action="$1"
    case "$action" in
        list)
            # JSON格式输出包列表
            ;;
        install)
            # JSON格式输出安装结果
            ;;
        info)
            # JSON格式输出包详情
            ;;
        deps)
            # JSON格式输出依赖检查结果
            ;;
        *)
            die "未知API操作: $action"
            ;;
    esac
}
```

### 4. 具体实施步骤

#### 第一阶段：Shell API实现
1. 在 kuraliAll.sh 中添加 `api` 命令
2. 实现JSON输出格式
3. 确保模块化调用

#### 第二阶段：WebUI重构
1. 修改WebUI调用Shell API
2. 删除重复的Python实现
3. 保留前端界面和文件上传逻辑

#### 第三阶段：集成测试
1. 验证API调用正确性
2. 测试所有功能
3. 优化错误处理

## 优势
1. **减少重复代码** - WebUI不再维护独立的包处理逻辑
2. **统一行为** - 两个版本功能一致
3. **易于维护** - Shell版本修改自动影响WebUI
4. **更好的错误处理** - 统一的错误消息

## 挑战
1. **JSON输出格式设计** - 需要标准化数据格式
2. **进程调用开销** - 每个WebUI操作都要调用Shell
3. **错误传递** - Shell错误需要正确传递给WebUI

## 实施方案

### 立即开始
1. 分析WebUI的包管理逻辑
2. 设计JSON输出格式
3. 实现Shell API模块